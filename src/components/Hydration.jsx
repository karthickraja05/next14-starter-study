

const Hydration = () => {
      const temp = Math.random();
      console.log(temp)
      return (
            <div>
                  {temp}
            </div>
      );
};

export default Hydration;